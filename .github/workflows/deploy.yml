name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:  # Allow manual testing

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Set Backblaze region
        run: echo "AWS_DEFAULT_REGION=us-west-004" >> $GITHUB_ENV

      - uses: actions/checkout@v4

      - name: Notify deployment starting
        if: ${{ secrets.DISCORD_WEBHOOK_URL != '' }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -s -X POST "$DISCORD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d '{"embeds":[{"title":"🚀 Deployment Starting","description":"Site rebuild triggered","color":3447003,"footer":{"text":"ChinaRxiv Translation Pipeline"}}]}'

      # Cache translations to avoid full re-download on every deploy
      - name: Cache translations
        uses: actions/cache@v4
        with:
          path: data/translated
          key: translations-${{ github.run_id }}
          restore-keys: |
            translations-

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install pandoc and XeLaTeX for PDF generation
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-fonts-recommended fonts-noto-cjk
          which pandoc || { echo "❌ pandoc installation failed"; exit 1; }
          which xelatex || { echo "❌ xelatex installation failed"; exit 1; }
          pandoc --version | head -1

      - name: Hydrate translations from B2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.BACKBLAZE_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.BACKBLAZE_APPLICATION_KEY }}
          BACKBLAZE_S3_ENDPOINT: ${{ secrets.BACKBLAZE_S3_ENDPOINT }}
          BACKBLAZE_BUCKET: ${{ secrets.BACKBLAZE_BUCKET }}
          BACKBLAZE_PREFIX: ${{ secrets.BACKBLAZE_PREFIX }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          set -e
          if [ -z "${AWS_ACCESS_KEY_ID}" ] || [ -z "${AWS_SECRET_ACCESS_KEY}" ] || [ -z "${BACKBLAZE_S3_ENDPOINT}" ] || [ -z "${BACKBLAZE_BUCKET}" ]; then
            echo "❌ Missing B2 credentials for hydration"
            python -m src.tools.b2_alerts add "deploy: hydrate skipped (missing credentials)"
            exit 1
          fi
          pip install awscli >/dev/null 2>&1
          DEST="s3://${BACKBLAZE_BUCKET}/${BACKBLAZE_PREFIX}"
          mkdir -p data/translated
          # Clear any cached data to ensure we don't mix stale translations with fresh ones
          rm -rf data/translated/*
          echo "⬇️  Syncing ${DEST}validated/translations → data/translated ..."
          # Sync must succeed - use if-then to capture failure despite set -e
          if ! aws s3 sync "${DEST}validated/translations" data/translated --delete --exclude "*" --include "*.json" --endpoint-url "${BACKBLAZE_S3_ENDPOINT}" --only-show-errors; then
            echo "❌ B2 sync failed"
            python -m src.tools.b2_alerts add "deploy: B2 sync failed"
            python -m src.tools.b2_alerts flush || true
            exit 1
          fi
          COUNT=$(ls -1 data/translated/*.json 2>/dev/null | wc -l | tr -d ' ')
          echo "Found ${COUNT} validated translation files"
          if [ "${COUNT}" = "0" ]; then
            echo "❌ No validated translations found after hydration"
            python -m src.tools.b2_alerts add "deploy: zero validated translations"
            python -m src.tools.b2_alerts flush || true
            exit 1
          fi
          # Download figure manifest
          echo "⬇️  Downloading figure manifest from B2..."
          aws s3 cp "${DEST}figures/manifest.json" data/figure_manifest.json --endpoint-url "${BACKBLAZE_S3_ENDPOINT}" || echo "⚠️ No figure manifest found - site will render without translated figures"
          if [ -f data/figure_manifest.json ]; then
            FIG_COUNT=$(python -c "import json; d=json.load(open('data/figure_manifest.json')); print(len(d.get('papers', {})))")
            echo "📊 Figure manifest: ${FIG_COUNT} papers with translated figures"
          fi

      - name: Render site
        env:
          BACKBLAZE_S3_ENDPOINT: ${{ secrets.BACKBLAZE_S3_ENDPOINT }}
          BACKBLAZE_KEY_ID: ${{ secrets.BACKBLAZE_KEY_ID }}
          BACKBLAZE_APPLICATION_KEY: ${{ secrets.BACKBLAZE_APPLICATION_KEY }}
          BACKBLAZE_BUCKET: ${{ secrets.BACKBLAZE_BUCKET }}
          BACKBLAZE_PREFIX: ${{ secrets.BACKBLAZE_PREFIX }}
        run: |
          python -m src.render
          python -m src.search_index
          # Count generated PDFs
          PDF_COUNT=$(find site/items -name "*.pdf" 2>/dev/null | wc -l | tr -d ' ')
          echo "📄 Generated ${PDF_COUNT} English PDFs"
          if [ "${PDF_COUNT}" = "0" ]; then
            echo "⚠️ WARNING: Zero English PDFs generated - check pandoc/pdflatex availability"
          fi

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          npm install -g wrangler
          wrangler pages deploy site --project-name chinarxiv

      - name: Purge Cloudflare cache
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
        run: |
          if [ -z "${CF_ZONE_ID}" ]; then
            echo "⚠️ CF_ZONE_ID not set - skipping cache purge"
            exit 0
          fi
          echo "🧹 Purging Cloudflare cache..."
          curl -s -X POST "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/purge_cache" \
            -H "Authorization: Bearer ${CF_API_TOKEN}" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything":true}' | jq -r '.success // "failed"'

      - name: Flush B2 alert buffer (if any)
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -m src.tools.b2_alerts flush || true

      - name: Notify deployment complete
        if: ${{ success() && secrets.DISCORD_WEBHOOK_URL != '' }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -s -X POST "$DISCORD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d '{"embeds":[{"title":"✅ Deployment Complete","description":"Site is live at chinarxiv.org","color":3066993,"footer":{"text":"ChinaRxiv Translation Pipeline"}}]}'

      - name: Notify deployment failed
        if: ${{ failure() && secrets.DISCORD_WEBHOOK_URL != '' }}
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -s -X POST "$DISCORD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d '{"embeds":[{"title":"❌ Deployment Failed","description":"Check workflow logs for details","color":15158332,"footer":{"text":"ChinaRxiv Translation Pipeline"}}]}'
