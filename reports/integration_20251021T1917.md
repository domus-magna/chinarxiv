# Integration Test Run — 2025-10-21T19:17Z

## Run Overview
- **Run ID**: `20251021T1917`
- **Goal**: Full production integration test using existing credentials (BrightData, OpenRouter, Backblaze B2, Cloudflare).
- **Scope**: Harvest → PDF/OCR → Translation → QA gates → Render/Search → B2 publish & hydrate → Cloudflare preview → Cleanup/reporting.

## Step 1 — Preflight with Production Credentials
- `python -m src.tools.env_diagnose --preflight` loaded `OPENROUTER_API_KEY` from `.env` and reported ✅ preflight.
- Tool availability confirmed:
  - `aws-cli/1.42.55` (installed in virtualenv)
  - `tesseract 5.5.1`
  - `ocrmypdf 16.8.0`
  - `npx wrangler 4.42.1`
- Virtual environment refreshed (`python -m venv .venv` + `pip install -r requirements.txt` + `pip install awscli`).
- Outcome: Ready to proceed with live API calls.

